import React from 'react';
import { Card, Button } from 'react-bootstrap';

function CartCard({ item ,sellerId }) {

  const redirectToSellerItems = (sellerId) => {
    console.log(sellerId)
      };

  return (
    
  <></>
  );
}

export default CartCard;
